#include "main.h"

/**
 * _isupper - check id a letter us upper
 * @x: The number to be checked
 * Return: 1 for upper letter or 0 fot else
 */

int _supper(int x)
{
	if (x >= 65 && x <= 90)
	{
	return (1);
	}
	return (0);
}
