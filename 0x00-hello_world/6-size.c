#include <stdio.h>
/**
 * main -A program that prints the size of various types on the computer types
 * return 0 (Success)
 */
int main(void)
{
	char a;
	int b;
	long int c;
	long long int d;
	float f;
printf("Size of a char : % lu byte(s)\n *, sizeof(a));
printf("Siz of an int : % lu byte(s)\n *, sizeof(b));
printf("Size of a long int : % lu bytes(s)\n *, sizeof(c));
printf("Siz of a long long int : % lu bytes(s)\n *, sizeof(d));
printf("Size of a float : % lu bytes(s)\n *, sideof(f));
return (0);
}
